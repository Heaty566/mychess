interface ILangItem {
        label: string;
        key: string;
}

const config: Array<ILangItem> = [
        { label: 'Vietnamese', key: 'vi' },
        { label: 'English', key: 'en' },
];

export default config;
